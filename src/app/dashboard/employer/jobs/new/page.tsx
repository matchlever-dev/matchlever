import { JobOpeningCreatePage } from "@/components/employer/job-opening-pages";

export const metadata = {
  title: "New Job Opening | MatchLever",
  description: "Create a MatchLever job opening that mirrors talent preferences.",
};

export default function Page() {
  return <JobOpeningCreatePage />;
}
